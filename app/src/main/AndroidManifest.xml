<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.travelagent.pos">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TravelAgentPOS"
        tools:targetApi="31">

        <!-- Main Activity (Launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Customer Activities -->
        <activity
            android:name=".ui.CustomerListActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.AddCustomerActivity"
            android:exported="false"
            android:parentActivityName=".ui.CustomerListActivity" />

        <!-- Trip Activities -->
        <activity
            android:name=".ui.TripListActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.AddTripActivity"
            android:exported="false"
            android:parentActivityName=".ui.TripListActivity" />

        <activity
            android:name=".ui.TripDetailsActivity"
            android:exported="false"
            android:parentActivityName=".ui.TripListActivity" />

        <!-- Driver & Vehicle Activity -->
        <activity
            android:name=".ui.DriverVehicleActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Booking & Ticket Activities -->
        <activity
            android:name=".ui.BookingActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.TicketPrintActivity"
            android:exported="false"
            android:parentActivityName=".ui.BookingActivity" />

        <!-- FileProvider for sharing files (optional, for future Excel/PDF export) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>